{
  "name": "assembler",
  "version": "1.0.0",
  "description": "translate hack machine language into machine code",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "assemble": "ts-node -P ./tsconfig.json ./src/index.ts",
    "build": "tsc -P ./tsconfig.json && node ./dist/src/index.js -f ./add/Add.asm"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.9"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
